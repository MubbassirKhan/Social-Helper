# Go to ur project dir
cd app

# Create Virtual Environment
python -m venv venv

# Activate venv
.\venv\Scripts\activate

# Install Requirements
pip install -r requirements.txt

# Run app.py
python app.py
